---
description: Upload a MicroPython file to the CyberPi via the mBlock web IDE.
---

Follow the `upload-cyberpi` skill to upload the file given in $ARGUMENTS
(a local `.py` path, required — ask if missing). Do not guess the path.
